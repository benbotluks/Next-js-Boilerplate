import { DEFAULT_GAME_SETTINGS, NOTE_CONFIG } from '@/config/gameConfig';

// Re-export from centralized config
export const AVAILABLE_NOTES = NOTE_CONFIG.AVAILABLE_NOTES;
export const DEFAULT_SETTINGS = DEFAULT_GAME_SETTINGS;
